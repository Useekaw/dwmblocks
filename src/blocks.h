#ifndef COMPONENTS_H
#define COMPONENTS_H

const int datetime(char *buffer, const char *arg);
const int battery(char *buffer, const char *bat);
const int battery_perc(char *buffer, const char *arg);
const int battery_remaining(char *buffer, const char *args);

#endif
